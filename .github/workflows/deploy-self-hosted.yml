name: Deploy (self-hosted)

on:
  push:
    branches: [ main ]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: self-hosted

    steps:
      - name: Executar Script de Deploy Local
        run: |
          # Chamamos o caminho absoluto do script usando sudo
          sudo /var/www/deploy.sh